'use strict';

angular.module('jenkins-workflow-demoApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'RESTX',
      'Twitter Bootstrap',
      'AngularJS',
      'Karma'
    ];
  });
